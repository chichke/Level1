import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textInput: {
    margin: "2%",
    padding: "5%", // Bigger button !
    borderRadius: 25, // Rounded corner, is this still a thing?
    backgroundColor: "#d3d3d3", // Everyone loves grey
  },
});
